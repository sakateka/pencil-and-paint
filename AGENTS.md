# Repository instructions

## Commits

- Use Conventional Commits for every commit message: `type(scope): description`
  when a scope is useful, or `type: description` otherwise.
- Keep the description concise, imperative, and lowercase.
- Commit each logical unit separately.
- Do not push unless the user explicitly asks for it.
